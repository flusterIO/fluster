import 'package:flutter/material.dart';

class BookmarksSideMenuPanel extends StatelessWidget {
  const BookmarksSideMenuPanel({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Bookmarks");
  }
}
