import 'package:flutter/material.dart';

class NoteByIdScreen extends StatelessWidget {
  const NoteByIdScreen({super.key, required this.id});
  final int id;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
