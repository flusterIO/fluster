import 'package:flutter/material.dart';

class NotesRouteRoot extends StatelessWidget {
  const NotesRouteRoot({super.key});
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
