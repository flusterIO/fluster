import PanelContainer from "@/components/util/panel_container";
import React, { type ReactNode } from "react";

export const AiChatPage = (): ReactNode => {
  return <PanelContainer>Ai Chat Page</PanelContainer>;
};

AiChatPage.displayName = "AiChatPage";
