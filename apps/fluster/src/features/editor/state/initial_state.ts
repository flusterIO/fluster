import { CodeState } from "./code_state";

export const initialCodeState: CodeState = {
    keymap: "standard",
    defaultLanguage: "python",
    theme: {
        dark: "dracula",
        light: "github-light",
    },
};
