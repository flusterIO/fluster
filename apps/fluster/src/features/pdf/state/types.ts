export enum PdfLoadingState {
    // The initial state before any loading has occurred.
    initial,
    // Loading, obviously.
    loading,
    // The pdf component is at rest and not loading anything.
    rest,
}
