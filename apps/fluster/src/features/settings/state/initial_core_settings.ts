import { CoreSettings } from "./core_settings";

// FIXME: Handle this notes_directory during the onboarding process once the input is in order.
export const initialCoreSettings: CoreSettings = {
    notesDirectory: "",
    hasLoadedSavedState: false,
};
