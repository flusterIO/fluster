import 'package:flutter/material.dart';

class BibliographySideMenuPanel extends StatelessWidget {
  const BibliographySideMenuPanel({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Bibliography");
  }
}
