{
    "tasks": [
        {
            "label": "Build grpc rust",
            "type": "shell",
            "command": "cd $FLUSTER_NATIVE_ROOT/packages/fluster_grpc && cargo build"
        },
        {
            "label": "Build grpc for all languages",
            "type": "shell",
            "command": "cd $FLUSTER_NATIVE_ROOT/packages/fluster_grpc && sh $FLUSTER_NATIVE_ROOT/packages/fluster_grpc/distribute_proto.sh"
        }
    ]
}
