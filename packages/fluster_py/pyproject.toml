[project]
name = "flusterpy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Fluster.IO", email = "uhlittlelessdumb@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "appdirs>=1.4.4",
    "lancedb>=0.25.2",
]

[project.scripts]
flusterpy = "flusterpy:main"

[build-system]
requires = ["uv_build>=0.9.3,<0.10.0"]
build-backend = "uv_build"
