[package]
name = "fluster_ts"
version = "0.0.1"
description = "Typescript bindings for the Fluster ecosystem."
authors = ["Andrew Mueller"]
license = "ISC"
edition = "2021"
exclude = ["index.node"]

[lib]
crate-type = ["cdylib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
neon = "1"
